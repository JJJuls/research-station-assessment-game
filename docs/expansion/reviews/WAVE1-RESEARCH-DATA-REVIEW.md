<!--
Review gate: Wave 1 (556e273..89e9597)
Reviewer: research-data-reviewer (project agent definition, model: sonnet, read-only)
Coordinator: Fable main agent (synthesis only)
Date: 2026-07-12
Repo state reviewed: HEAD e5adf6f (file-identical to 89e9597 for all game/docs/test paths)
Preserved verbatim from the reviewer's final report; no findings edited.
-->

# Wave 1 Research-Data Review — Remote Outpost Assessment

**Range reviewed**: `556e273..89e9597` (shared architecture U1–U6 + six station rooms: Systems Repair, Engineer Hub, Inventory/Prep, Side Repair Bay, Interruption Corridor, Final Core; Wave 1B runtime-verification evidence). Current tree (HEAD) confirmed file-identical to `89e9597` for all reviewed paths.

## 1. Scope

Reviewed against `docs/ai/fable-claude-final-game-build-contract-v3.txt` (V3) §1/3/4/5/6, `docs/research/event-schema.md`, and the room docs. Files read in full: all six changed room docs, `event-schema.md`, `CanonicalEventContext.ts`, `researchInteractions.ts`, `missionVocabulary.ts`, `duties.ts`, `ResearchRuntime.ts`, `SessionState.ts`, `RoomScene.ts`, `roomTaskState.ts`, `stationRegistry.ts`, all six new/changed scenes (`ArchiveScene`, `RepairScene`, `EngineerScene`, `InventoryScene`, `SideRepairScene`, `InterruptionScene`, `FinalCoreScene`, `HubScene`), `key.ts`, `ScoringManager.ts`, `EventLogger.ts`, `WAVE1B-EVIDENCE.md`, `ACTIVE-EXPANSION-STATE.md`, `proto-drive-report.json`, and two e2e specs (`repair_room_logging.spec.ts`, `final_core_summary.spec.ts`) as evidence samples. Confirmed via `Glob`/`Grep` that no Hazard scene exists and `key.ts` has no `hazard` scene key.

## 2. Q01–Q33 traceability

All newly registered `CANONICAL_EVENT_CONTEXT` entries for this wave (~40 entries covering Repair, Engineer Hub, Inventory, Side Repair, Interruption, Final Core) trace to Q-item rows explicitly listed in `MASTER_33_ALIGNMENT.md`'s Events columns, per the map's own population rule and per-entry source comments. Spot-checked against room docs and two e2e specs asserting live payloads — all match. `construct_id` is deliberately left `undefined` wherever a listing spans rows with different/mismatched valence (e.g. `issue_resolution_attempted`, `final_core_rushed`, `repair_abandoned`) rather than guessed — **pass**. Station Hub events correctly carry `study_item_ids: []` and no `construct_id`, excluded from scoring — **pass**, matches control/usability labelling rule (V3 §1 rule 1).

No untraced or silently-mislabelled event/variable found in the diff.

## 3. Wording check

Reviewed all option labels, NPC dialogue, and prompt bodies added/changed in Engineer Hub, Inventory/Prep, Side Repair, Interruption, Final Core, and Repair scenes. All stay in-fiction ("Submit a quick report from memory", "Grab tools quickly without checking the list", "Work through the difficulty and complete the repair", etc.) — no Q01-Q33 item wording restated. **Pass.**

## 4. Persistence separation check

**Pass.** No code path merges adaptive and inappropriate persistence. `ScoringManager.computeSummary()` (unchanged in this range) keeps `game_difficulty_persistence`/`failure_adaptation_index` (adaptive) structurally separate from `game_inappropriate_persistence`/`blind_retry_count` (maladaptive); the e2e evidence (`repair_room_logging.spec.ts`) runtime-verifies both directions: the adaptive path leaves `game_inappropriate_persistence = 0`/`blind_retry_count = 0`, the blind-retry path sets both to exactly 1. New Final Core duty/force-continue events (`accepted_duty_unresolved`, `final_core_force_continue`) stay in their own `responsibility`/`inappropriate_persistence` construct lanes, never blended into a single mixed score.

## 5. Exploratory proxy labelling check

**Pass.** `goal_time_exploratory` and `consistency_of_interest_exploratory` `construct_id` suffixes are applied correctly and consistently to every Q29/Q30/Q32/Q17-Q20-linked event added this wave (`stabiliser_option_offered`, `stabiliser_accepted`, `final_bonus_unlocked`, `inventory_verified_complete`, `side_repair_first_step`, `side_repair_abandoned_after_start`, `competing_task_viewed`, `switched_task`, `returned_to_original_task`, `objective_active`, `new_goal_offered`, `goal_switch_accepted`, `prior_goal_completed`, `prior_goal_abandoned`, `final_core_stability_bonus`, `final_core_issue_resolved`). Room docs' Scoring-notes sections restate the exploratory/optional caveat for every one of these. Q04 stays cleanup/disorder only in Inventory/Prep (`workspace_left_disordered`/`cleanup_completed`), never planning-before-acting — explicit doc guard present and code matches it.

## 6. Data integrity check

- **Payload completeness**: `RoomScene.logRoomEvent()` supplies `room_id`, `task_id`, `object_id`, `x`/`y`, `study_item_ids`, `construct_id`, `success` from the canonical/interaction registries; `ResearchRuntime.logInteraction()` appends `session_id`, `timestamp_ms`, `participant_id`, `game_session_id`, `condition`, `game_version`, `elapsed_seconds` with caller fields spread first so session-context fields can never be overridden — consistent with V3 §3.2 minimum payload. `EventLogger`'s `RawGameEvent` interface (unchanged this range, legacy `scene`/`state_before`/`state_after` shape) is a documented, pre-existing migration gap, not something this wave introduced or worsened.
- **Raw-vs-summary / append-only**: `EventLogger.log()` only pushes; `getEvents()`/`toJSON()` only read and defensively copy (including a metadata deep-copy). `ScoringManager.computeSummary()` only reads `events` via `Array.filter`. No mutation, truncation, or filter-in-place path found anywhere in the diff — **pass** (event-schema.md §3 rule holds).
- **SessionState**: all eleven V3 §3.1 fields present in `MissionState`; new writer methods (`addAcceptedDuty`/`addSkippedDuty`/`addPreparedItem`/`setWorkspaceStatus`/`setSideRepairStatus`/`setInterruptionStatus`/`setFinalCoreStatus`) are correctly invoked from the six new rooms' `onSelected` handlers, and `getMissionState()` returns a defensive deep copy (confirmed by inspecting `copyMissionState`). — **pass**.
- **QualtricsBridge/session metadata**: not touched this range; `ResearchRuntime`'s only change is the additive, dev-only, read-only `getMissionState()` debug method — does not affect the 6 baseline debug methods, raw logs, or Qualtrics return flow.
- **DataQualityTracker**: unchanged; wired session-globally via `researchRuntime.dataQualityTracker.start()`/`.getMetrics()`, so it automatically covers the six new rooms without room-specific wiring — **pass**, nothing bypasses it.

## 7. Evidence completeness (WAVE1B-EVIDENCE.md / proto-drive-report.json)

Spot-checked two of the six new specs (`repair_room_logging.spec.ts`, `final_core_summary.spec.ts`) against both the evidence doc's claims and `CanonicalEventContext.ts`: every asserted `study_item_ids`/`construct_id`/`success` triple in the specs matches the registry exactly, including deliberately-unset-construct cases (`repair_abandoned`, `final_core_rushed`, `issue_resolution_attempted`). `proto-drive-report.json` contains real `computeSummary()` output with zero diffs on stable fields for the three prototype-regression sessions, substantiating the "0 diffs" claim in §5 of the evidence doc. The evidence doc's claim of "zero diff to `CanonicalEventContext.ts`/`ScoringManager.ts`/`QualtricsBridge.ts`/`EventLogger.ts`/`SessionState.ts`/`DataQualityTracker.ts`" is independently corroborated by the task's own changed-file list, which excludes all six of those files. No claim found in WAVE1B-EVIDENCE.md or the room docs without a corresponding runtime/log artifact. **Pass.**

## 8. Blocked-decision / Hazard Control integrity

- `hazard_avoidance` canonical resolution, idle threshold, `task_started` Q-listing conflict, abandon/return `construct_id` (Q24/Q25), `engineer_report_submitted_supervised` mapping, `interruption_alert_acknowledged` mapping, and the Beat-13 scoring bundle are all still listed as open/user-owned in `ACTIVE-EXPANSION-STATE.md` "Unresolved issues" — none silently resolved by this wave's code.
- **Hazard Control confirmed unimplemented**: `Glob` for `src/scenes/*Hazard*` returns nothing; `key.ts`'s `scene` map has no `hazard` key; `stationRegistry.ts`'s `hazard_control_room` entry has no `sceneKey` (Hub door renders sealed) and no `statusBoardLabel`. — **pass**, matches check 14.

## 9. Issues found

| #   | Finding                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Class                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Contract rule |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------- |
| 1   | `game_inappropriate_persistence` in `ScoringManager.ts` (unchanged this range) still only sums `archive_same_wrong_code_repeated + repair_same_sequence_repeated + hazard_reckless_continue`; it does **not** include `final_core_force_continue`, even though that event is newly emitted this wave and V3 §6's composite formula explicitly requires it (`game_inappropriate_persistence = ... + final_core_force_continue`). A participant who forces through the Final Core blocker will not have that action reflected in the Qualtrics summary variable.                                                                       | **Major** (data-completeness gap against V3 §6), but explicitly disclosed in `docs/game/rooms/08-final-core-room.md` Failure/edge-cases and in `ACTIVE-EXPANSION-STATE.md` unresolved issue 6 ("Beat-13 scoring fixes bundle, out of Wave 1A scope by design") — not a silent omission, and `ScoringManager.ts` was correctly kept out of this wave's touched-file set pending a supervised scoring beat. Flagging so it is not lost before that beat. |
| 2   | `RepairScene`/`ArchiveScene` prompts (pre-existing, not modified in this range per `event-schema.md`'s pre-Wave-1A "exact" status for `repair_strategy_revision`/`archive_strategy_revision`) expose "Apply revised sequence"/"Try revised query" as always-available options — a player can select them as their very first action with no prior scripted failure, and the event still logs `success: true`, `construct_id: adaptive_persistence`, `study_item_ids` incl. Q23. This is a construct-validity risk for a minority of playthroughs (revision credited without an antecedent setback) inherited from before this range. | **Minor / observation** — pre-existing, not introduced or worsened by Wave 1, no new file in this diff changes this behavior. Flagging for future-beat awareness only.                                                                                                                                                                                                                                                                                 |
| 3   | `final_core_blocker_shown` (Q28, `inappropriate_persistence`) is a passive system-display event fired at prompt-open, before the player has made any choice — assigning `inappropriate_persistence` to a "shown" event (rather than only to the player's subsequent force-continue action) is a light construct-validity wrinkle.                                                                                                                                                                                                                                                                                                    | **Observation** — follows the already-established `hazard_warning_seen` precedent (system-display event carries its single listing row's construct), consistently applied, not a new deviation invented this wave.                                                                                                                                                                                                                                     |
| 4   | None of the six new rooms' Playwright specs were reviewed exhaustively (only 2 of 6 sampled in depth); the remaining four (`engineer_hub_logging`, `inventory_prep_logging`, `side_repair_logging`, `interruption_corridor_logging`) were cross-checked only against `CanonicalEventContext.ts` mappings via the room docs and `WAVE1B-EVIDENCE.md`'s 24/24-pass claim, not read line-by-line.                                                                                                                                                                                                                                       | **Observation** — scope-of-review note, not a defect; the two sampled specs and the evidence doc's consolidated pass count give reasonable assurance the remaining four follow the same pattern.                                                                                                                                                                                                                                                       |

No blocker-class findings. No unlabelled exploratory proxy, no collapsed global score, no questionnaire-wording leakage, no mutation of raw logs, and no user-owned decision silently resolved were found anywhere in this range.

## 10. Verdict

**research-valid with minor notes** — Wave 1 (U1–U6 shared architecture + six station rooms + Wave 1B verification) preserves Q01-Q33 traceability, adaptive/inappropriate persistence separation, exploratory-proxy labelling, append-only raw logging, and SessionState/QualtricsBridge integrity throughout. The one **major**-class finding (finding 1) is a pre-existing, explicitly-disclosed ScoringManager gap correctly deferred to a future supervised scoring beat, not a defect introduced or concealed by this wave. Hazard Control remains genuinely unimplemented and all user-owned scientific decisions remain correctly recorded as blocked.
