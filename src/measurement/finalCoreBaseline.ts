/**
 * NEXT-10 Final Core baseline constants (adopted ruling, 6154a82; Unit 3).
 *
 * Every participant receives one identical baseline issue/blocker
 * component in the Final Core review, regardless of prior performance:
 * it guarantees the blocker display and the force option exist for every
 * session (the force option must not appear only when participant-created
 * issues exist), standardising the Q11/Q28 entry state. The version tag
 * is recorded internally; Q11/Q28 event ownership and scoring stay OPEN.
 */

export const FINAL_CORE_BASELINE_VERSION = 'fc-baseline-v1';

export const FINAL_CORE_BASELINE_ISSUE_LABEL =
  'core sync buffer flag pending review';
